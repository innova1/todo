<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        <link rel='stylesheet' href='/stylesheets/style.css' />
        <link rel='stylesheet' href='/stylesheets/fbker.css' />
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/fontawesome-all.css">
        
    </head>
    
    <body>
        <div class="container">
    
            <% include ./partials/navfbk %>
            <br />
                <%  let fbk = {}; %>
                <% if (myFbksIn.length == 0) { %>
                    <h5 style="color: #007bff;">No feedback gifts to display. Maybe you should ask for some?</h5>
                <%} else {%>
                    <h5 class="section" style="color: #007bff;"><span style="display:inline;"><i class="fas fa-caret-down"></i></span><span style="display:none;"><i class="fas fa-caret-right"></i></span> Hey, <%=myFbksIn[0].fbkee.shortname%>, here's the feedback you've received<span style="color: red; font-size: 14px; font-style: italic; display: none">&npsp;(Click to open)</span></h5>
                    <div>
                    <% for(let i = 0; i < myFbksIn.length; i++) { 
                        fbk = myFbksIn[i] %>
                        <% include ./partials/showfbk %>
                    <% } %>
                    </div>
                <% } %>

            <div id="fbksOut">
                <%  fbk = {}; %>
                <% if (myFbksOut.length == 0) { %>
                    <h5 style="color: #007bff;">You haven't given any feedback gifts. Anyone you want to acknowledge?</h5>
                <%} else {%>
                <h5 class="section" style="color: #007bff;"><span style="display:inline;"><i class="fas fa-caret-down"></i></span><span style="display:none;"><i class="fas fa-caret-right"></i></span> Here's the feedback you've given <span style="color: red; font-size: 14px; font-style: italic; display: none">&nbsp;(Click to open)</span></h5>
                <div>
                    <% for(let i = 0; i < myFbksOut.length; i++) { 
                        fbk = myFbksOut[i] %>
                        <% include ./partials/showfbk %>
                    <% } %> 
                </div>
                <% } %>
                <p class="font-weight-light font-italic">Served by <%= hostname %></p>
            </div>
        </div> <!-- container -->

        <script src="/js/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.6/umd/popper.min.js" integrity="sha384-wHAiFfRlMFy6i5SRaxvfOCifBUQy1xHdJ/yoi7FRNXMRBu5WHdZYu1hA6ZOblgut" crossorigin="anonymous"></script>
        <script src="/js/bootstrap.min.js"></script>

        <script>
            $(document).ready( function () {
                //$('.section').children("span").hide();
                $('.section').click(function() {
                    jQuery(this).next("div").toggle();
                    jQuery(this).children("span").toggle();
                });
            });
        </script>

    </body>
</html>
