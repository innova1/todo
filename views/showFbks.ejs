<!DOCTYPE html>
<html>
    <head>
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/fontawesome-all.css">
    </head>
    
    <body>
        <div class="container">
            <% include ./partials/navfbk %>
            <br />
                <%  let fbk = {}; %>
                <% if (myFbksIn.length == 0) { %>
                    <h5 style="color: #007bff;">No feedback gifts to display. Maybe you should ask for some?</h5>
                <%} else {%>
                    <h5 class="section" style="color: #007bff;">Hey, <%=myFbksIn[0].fbkee.shortname%>, here's the feedback you've received <span class="fbksIn">(hidden)</span></h5>
                    <div class="fbksIn">
                    <% for(let i = 0; i < myFbksIn.length; i++) { 
                        fbk = myFbksIn[i] %>
                        <% include ./partials/showfbk %>
                    <% } %> 
                    </div>
                <% } %>

            <div id="fbksOut">
                <%  fbk = {}; %>
                <% if (myFbksOut.length == 0) { %>
                    <h5 style="color: #007bff;">You haven't given any feedback gifts. Anyone you want to acknowledge?</h5>
                <%} else {%>
                <h5 class="section" style="color: #007bff;">Here's the feedback you've given <span class="fbksOut">(hidden)</span></h5>
                <div class="fbksOut">
                    <% for(let i = 0; i < myFbksOut.length; i++) { 
                        fbk = myFbksOut[i] %>
                        <% include ./partials/showfbk %>
                    <% } %> 
                </div>
                <% } %>
                <p class="font-weight-light font-italic">Served by <%= hostname %></p>
            </div>
        </div> <!-- container -->

        <script src="/js/jquery.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>

        <script>
            $(document).ready( function () {
                $('span.fbksIn').hide();
                $('span.fbksOut').hide();
                $('.section').click(function() {
                    c = $(this).attr("class");
                    console.log("clicked with class " + c);
                    $("." + c).toggle();
                });
            });
        </script>

    </body>
</html>
