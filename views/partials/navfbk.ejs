<nav class="navbar navbar-expand-lg navbar-dark bg-primary">

    <a class="navbar-brand" href="/">The Gift of Feedback</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-item nav-link active" href="/">List Feedback</a>
            </li>
            <li class="nav-item">
                <a class="nav-item nav-link active" href="/task/add">Add Feedback</a>
            </li>
            <li class="nav-item">
                <a class="nav-item nav-link active" href="/scoreboard">Scoreboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-item nav-link active" href="/faq">FAQ</a>
            </li>
            <li class="nav-item">
                <a class="nav-item nav-link active" href="/logout">Logout</a>
            </li>
        </ul>
    </div>
</nav>
<div class="container">
    <%if (typeof inCount === 'undefined' ) {%>
        <!--show nothing-->
    <%} else {%>
        <div id="scorebadge" >            
            <button type="button" class="btn btn-warning" style="padding-top: 9px; padding-bottom: 9px;">
                In&nbsp;<span class="badge badge-light"><%=inCount%></span>&nbsp;&nbsp;Out&nbsp;<span class="badge badge-light"><%=outCount%></span>&nbsp;&nbsp;Score&nbsp;<span class="badge badge-light"><%=outScore%></span>
            </button>
            <%if ( isNoRatingIn ) {%>
                <div id="noRating">Unrated feedback</div>
            <%}%>
        </div>
    <%}%>
        <div >
    <%if ( typeof loggedInEmail != 'undefined' && loggedInEmail == 'tom.boulet@exxonmobil.com') {%>
        <%if ( typeof theFilter == 'undefined' || !theFilter[0] || theFilter[0] == '' ) {%>
            <div id="filterform">
                <form action="/filter" method="get">
                    <span class="badge badge-success">&nbsp;<input type="text" name="filter" size="10" /><button type="button" class="btn btn-success" >Filter</button></span>    
                </form>
            </div>
        <%} else {%>
            <div id="filterset">
                Filtered on: <div class="boxed"><%=theFilter.toString()%></div> <input type="button" class="btn btn-primary" onclick="location.href='/'" value="Clear" />
            </div>
        <%}
    }%>
        </div>
    </div>
</div>
